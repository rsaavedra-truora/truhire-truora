# Truora DS — component index

<!-- GENERATED by `pnpm ds:build` from the component sources — never edit by hand;
     `pnpm gate` fails on drift. One row per component: what it is, how to import
     it, and its .prompt.md usage card (read the card before using a component). -->

12 components, all published in **`@raandino/atelier-react`**
(tokens ship as `@raandino/atelier-tokens`). Throwaway HTML artifacts can instead
link `styles.css` and copy the JSX from each usage card.

## Brand

| Component | What it is | Import | Usage card |
|---|---|---|---|
| **BrandShape** | Brand graphic shapes ("ADN en formas") | `import { BrandShape } from '@raandino/atelier-react';` | [BrandShape.prompt.md](components/brand/BrandShape.prompt.md) |
| **ProductIcon** | Truora product icons built from the logo's curves | `import { ProductIcon } from '@raandino/atelier-react';` | [ProductIcon.prompt.md](components/brand/ProductIcon.prompt.md) |

## Core

| Component | What it is | Import | Usage card |
|---|---|---|---|
| **Avatar** | Circular user avatar with photo or initials fallback and optional presence dot | `import { Avatar } from '@raandino/atelier-react';` | [Avatar.prompt.md](components/core/Avatar.prompt.md) |
| **Badge** | Pill-shaped status / category label | `import { Badge } from '@raandino/atelier-react';` | [Badge.prompt.md](components/core/Badge.prompt.md) |
| **Button** | Truora pill action button | `import { Button } from '@raandino/atelier-react';` | [Button.prompt.md](components/core/Button.prompt.md) |
| **Card** | White surface container for grouped content | `import { Card } from '@raandino/atelier-react';` | [Card.prompt.md](components/core/Card.prompt.md) |
| **GlassCard** | Truora's signature Glass Effect surface | `import { GlassCard } from '@raandino/atelier-react';` | [GlassCard.prompt.md](components/core/GlassCard.prompt.md) |
| **IconButton** | Square icon-only button for toolbars and dense controls | `import { IconButton } from '@raandino/atelier-react';` | [IconButton.prompt.md](components/core/IconButton.prompt.md) |

## Forms

| Component | What it is | Import | Usage card |
|---|---|---|---|
| **Checkbox** | Checkbox with optional inline label | `import { Checkbox } from '@raandino/atelier-react';` | [Checkbox.prompt.md](components/forms/Checkbox.prompt.md) |
| **Input** | Labeled text field with helper/error text and optional adornments | `import { Input } from '@raandino/atelier-react';` | [Input.prompt.md](components/forms/Input.prompt.md) |
| **Switch** | On/off toggle for instant-apply settings | `import { Switch } from '@raandino/atelier-react';` | [Switch.prompt.md](components/forms/Switch.prompt.md) |

## Navigation

| Component | What it is | Import | Usage card |
|---|---|---|---|
| **Tabs** | Underline tab bar for switching views within a screen | `import { Tabs } from '@raandino/atelier-react';` | [Tabs.prompt.md](components/navigation/Tabs.prompt.md) |
