# Wireframe — <surface name>

<!-- Structure only. No colors, no fonts, no CSS — visual decisions live in the
     design system; this file owns the SHAPE. Filled in by the atelier
     wireframe-first interview (or by hand) and confirmed before any styling.
     Keep these headings as-is for cross-agent portability. -->

## Purpose

<!-- One or two lines: what this surface is for, and the single most important
     thing a user should accomplish on it. -->

## Users

<!-- Who uses it, in what context (operator at a desk, signer on a phone, …). -->

## Screens

<!-- Numbered list — every screen, view, or step. One line each. -->

1.
2.

## Regions

<!-- Per screen: an indented text wireframe, top to bottom. Name the regions and
     what each contains — order and nesting ARE the layout decision. -->

### 1. <screen>

```
<region>            — <what it holds>
<region>
  <sub-region>
```

## Content blocks

<!-- The data/content each region shows: fields, columns, counts, copy slots.
     Real names, no lorem placeholders where the domain is known. -->

## Hierarchy

<!-- What dominates each screen — the one element a user should see first,
     and what is intentionally secondary. -->

## States

<!-- Empty · loading · error (and any partial/permission states) per screen. -->

## Density

<!-- marketing-airy or product-dense (or per-screen if mixed). This is a
     structure call — the spacing values themselves come from the tokens. -->

## Open questions

<!-- Anything assumed rather than answered — mark interview assumptions here. -->
